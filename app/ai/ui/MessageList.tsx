
export const MessageList = async () => {
  return (
    <ul>
      {/* {counter} */}
      {/* {messages.map((msg) => {
        return <Message key={msg.id} {...msg} />;
      })} */}
    </ul>
  );
};
